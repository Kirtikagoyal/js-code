"use strict"; // treat all JS code as newer version

// alert( 3 + 3 ) we're using nodejs, not browser/dev console/ engine of js is hidden in browser.
//readibility is imp/avoid semicolon in nodejs,react
console.log(3 + 3)

console.log('kirtika')
let name = "kirtika"  //string datatype => ""
let age = "19"     //no. datatype range => 2 to power 53, bigint for big no's.
let isLoggedIN = false //boolean datatype  =>true/false
// null is a datatype and stand alone value (repst of empty value) 
// undefined => var is declared but no value is assigned
// symbol- to find uniqueness, implementation in react when we build so many components.
// object datatype

console.log(typeof undefined); //type
console.log(typeof null); //object
